import Foundation

func solution(_ numbers:[Int]) -> [Int] {
    
    // let answer_list = numbers.map({(data:Int) -> Int in return data * 2})
    
    let answer_list = numbers.map({(data) -> Int in data * 2 })
    
    // let answer_list = numbers.map({ $0 * 2})
    
    
    
    
    
    return answer_list
}